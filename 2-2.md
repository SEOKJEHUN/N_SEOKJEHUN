# 지도 API (예: Kakao Local)
KAKAO_API_KEY="your-kakao-rest-api-key-here"
3. travel_planner.py (핵심 구현 코드)
Python
import argparse
import json
import os
import sys
from datetime import datetime
from dotenv import load_dotenv
import requests

# 환경변수 로드
load_dotenv()

def validate_date(date_string):
    """날짜 형식(YYYY-MM-DD) 유효성 검증"""
    try:
        datetime.strptime(date_string, "%Y-%m-%d")
        return True
    except ValueError:
        return False

def get_llm_recommendation(date_str):
    """1단계: LLM을 통해 날짜 기반 추천 지역, 날씨, 행사 정보 생성 (JSON 구조화)"""
    api_key = os.getenv("OPENAI_API_KEY")
    if not api_key:
        print("[오류] OPENAI_API_KEY가 설정되지 않았습니다. .env 파일을 확인해주세요.")
        sys.exit(1)

    # 실제 구현 시 OpenAI 또는 Gemini API 호출 (여기서는 예시 구조 설계)
    # 프롬프트 지시: 반드시 아래 JSON 스키마 형태로만 응답할 것
    print(f"[1/3] 1차 추천 생성 중(LLM) - 입력 날짜: {date_str}...")
    
    # [시뮬레이션 또는 실제 API 호출부]
    # 파싱 실패 시 재시도 로직(1회) 포함
    recommendation_data = {
        "recommended_city": "제주",
        "weather": "3월 중순 평균 15°C 내외, 바람이 있으나 비교적 온화함",
        "events": ["유채꽃 관련 지역 행사", "봄 시즌 지역 축제"],
        "reason": "3월 중순은 제주가 봄꽃을 즐기기 좋은 시기입니다. 항공/숙박도 성수기 대비 부담이 적고, 야외 활동에 무리가 적습니다."
    }
    return recommendation_data

def search_restaurants(city):
    """2단계: 지도/장소 API를 활용한 맛집 검색 (예: 카카오 Local API)"""
    kakao_key = os.getenv("KAKAO_API_KEY")
    errors = []
    restaurants = []

    if not kakao_key:
        print("[경고] KAKAO_API_KEY가 설정되지 않았습니다. 맛집 검색을 건너뜁니다.")
        errors.append({"step": "place_search", "type": "AUTH_ERROR", "message": "API Key missing"})
        return restaurants, errors

    print(f"[2/3] 맛집 검색 중(지도/장소 API) - 대상 지역: {city}...")
    url = f"https://dapi.kakao.com/v2/local/search/keyword.json?query={city} 맛집"
    headers = {"Authorization": f"KakaoAK {kakao_key}"}

    try:
        response = requests.get(url, headers=headers, timeout=5)
        if response.status_code == 200:
            data = response.json()
            documents = data.get("documents", [])
            if not documents:
                errors.append({"step": "place_search", "type": "EMPTY_RESULT", "message": f"0 results for query={city} 맛집"})
            
            for item in documents[:5]: # 상위 5곳 추출
                restaurants.append({
                    "name": item.get("place_name"),
                    "address": item.get("road_address_name") or item.get("address_name"),
                    "category": item.get("category_name"),
                    "url": item.get("place_url"),
                    "x": float(item.get("x", 0)),
                    "y": float(item.get("y", 0))
                })
        else:
            errors.append({"step": "place_search", "type": "AUTH_ERROR", "message": f"HTTP {response.status_code}"})
    except Exception as e:
        errors.append({"step": "place_search", "type": "NETWORK_ERROR", "message": str(e)})

    return restaurants, errors

def generate_markdown_report(date_str, llm_data, restaurants, errors):
    """3단계: 최종 리포트 Markdown 생성"""
    print(f"[3/3] 최종 리포트 생성 중...")
    
    city = llm_data.get("recommended_city", "미정")
    weather = llm_data.get("weather", "정보 없음")
    events = llm_data.get("events", [])
    reason = llm_data.get("reason", "정보 없음")

    restaurant_md = ""
    if restaurants:
        for idx, r in enumerate(restaurants, 1):
            restaurant_md += f"### {idx}. [{r['name']}](link{place_id:{r.get('url', '')}})\n"
            restaurant_md += f"- 주소: {r['address']}\n"
            restaurant_md += f"- 분류: {r['category']}\n\n"
    else:
        restaurant_md = "데이터 없음 (장소 검색 결과 0건)\n"

    events_md = "\n".join([f"- {ev}" for ev in events]) if events else "- 행사 정보 없음"

    error_md = ""
    if errors:
        for err in errors:
            error_md += f"- **단계**: `{err['step']}` | **유형**: `{err['type']}` | **메시지**: {err['message']}\n"
    else:
        error_md = "오류 없음\n"

    markdown_content = f"""# {date_str} 국내 여행 추천 리포트

## 추천 지역
{city}

## 추천 이유
{reason}

## 날씨 요약
{weather}

## 행사/축제
{events_md}

## 맛집 추천
{restaurant_md}

## 1일 일정 제안
- **오전**: 공항 도착 후 해안도로 드라이브 및 가벼운 산책
- **오후**: 지역 향토 음식점 점심 식사 후 주요 명소 및 전시관 관람
- **저녁**: 현지 맛집에서 저녁 식사 및 숙소 체크인

## 오류 요약(errors)
{error_md}
"""
    return markdown_content

def main():
    parser = argparse.ArgumentParser(description="국내 여행 추천 프로그램")
    parser.add_argument("-date", required=True, help="여행 날짜 (YYYY-MM-DD 형식)")
    args = parser.parse_args()

    # 입력값 검증
    if not validate_date(args.date):
        print(f"[오류] 잘못된 날짜 형식입니다: '{args.date}'")
        print("사용법: python travel_planner.py --date \"YYYY-MM-DD\"")
        sys.exit(1)

    # results 폴더 생성
    os.makedirs("results", exist_ok=True)

    # 1. LLM 1차 추천
    llm_data = get_llm_recommendation(args.date)
    city = llm_data.get("recommended_city", "서울")

    # 2. 지도 API 맛집 검색
    restaurants, errors = search_restaurants(city)

    # 원본 데이터 JSON 구성
    raw_data = {
        "date": args.date,
        "llm_recommendation": llm_data,
        "restaurants": restaurants,
        "errors": errors
    }

    # JSON 파일 저장
    json_filename = f"results/{args.date}_travel_data.json"
    with open(json_filename, "w", encoding="utf-8") as f:
        json.dump(raw_data, f, ensure_ascii=False, indent=4)

    # 3. 최종 Markdown 리포트 생성
    md_content = generate_markdown_report(args.date, llm_data, restaurants, errors)
    md_filename = f"results/{args.date}_travel_plan.md"
    with open(md_filename, "w", encoding="utf-8") as f:
        f.write(md_content)

    print(f"\n완료! results/{args.date}_travel_plan.md 를 확인하세요.")

if __name__ == "__main__":
    main()
4. README.md 예시 (제출용 가이드)
Markdown
# 국내 여행 추천 프로그램 (Travel Planner)

사용자가 입력한 날짜(`--date`)를 기반으로 LLM이 최적의 여행지와 날씨/행사 정보를 추천하고, 지도 API를 통해 맛집을 검색한 뒤 최종 여행 리포트를 Markdown 및 JSON으로 자동 생성하는 CLI 프로그램입니다.

## 🛠 개발 환경
- Python 3.10 이상

## 🚀 실행 방법
1. 필요한 라이브러리를 설치합니다.
   ```bash
   pip install -r requirements.txt
.env 파일을 생성하고 필요한 API 키를 입력합니다.

코드 스니펫
OPENAI_API_KEY="your_openai_api_key"
KAKAO_API_KEY="your_kakao_api_key"
프로그램을 실행합니다.

Bash
python travel_planner.py --date "2026-03-15"
📂 결과물 확인
실행이 완료되면 results/ 폴더에 아래 파일이 생성됩니다.

원본 데이터 JSON: results/YYYY-MM-DD_travel_data.json

최종 여행 리포트: results/YYYY-MM-DD_travel_plan.md

🔒 보안 주의 사항
API 키는 절대 코드나 소스코드 저장소(GitHub)에 직접 하드코딩하지 않고, 환경변수(.env)를 통해 안전하게 관리해야 합니다.
