1. 파이썬 프로그램 소스 코드 (prompt_manager.py)
아래 코드를 복사하여 prompt_manager.py 파일로 저장하고 실행하시면 됩니다.

Python
# 초기 프롬프트 데이터 (최소 3개 이상 기본 등록)
prompts = [
    {
        "title": "블로그 글 작성 도우미",
        "content": "당신은 10년 경력의 전문 블로거입니다. 주어진 주제에 대해 SEO에 최적화된 블로그 글을 작성해주세요.",
        "category": "텍스트 생성",
        "favorite": True
    },
    {
        "title": "제품 썸네일 생성",
        "content": "다음 제품의 매력적이고 시선을 사로잡는 썸네일 이미지 생성을 위한 상세한 키워드와 프롬프트를 작성해주세요.",
        "category": "이미지 생성",
        "favorite": False
    },
    {
        "title": "IT 컨설턴트 페르소나",
        "content": "너는 15년 차 대기업 IT 수석 컨설턴트야. 기술 도입을 고민하는 CEO의 눈높이에 맞춰 명확하고 논리적으로 조언해줘.",
        "category": "페르소나",
        "favorite": False
    }
]

# 미리 정의된 카테고리 목록
CATEGORIES = ["텍스트 생성", "이미지 생성", "영상 생성", "페르소나", "자동화", "기타"]

def show_menu():
    """메인 메뉴 출력 및 선택 번호 반환"""
    print("\n=== 나만의 프롬프트 관리 ===")
    print("1. 프롬프트 추가")
    print("2. 프롬프트 목록")
    print("3. 카테고리별 조회")
    print("4. 프롬프트 검색")
    print("5. 프롬프트 상세 보기")
    print("6. 즐겨찾기 관리")
    print("7. 즐겨찾기 목록")
    print("0. 종료")
    choice = input("선택: ").strip()
    return choice

def add_prompt():
    """새로운 프롬프트 추가 기능"""
    print("\n=== 프롬프트 추가 ===")
    
    # 제목 입력 (비어있으면 재입력)
    title = ""
    while not title:
        title = input("제목: ").strip()
        if not title:
            print("제목은 비워둘 수 없습니다. 다시 입력해주세요.")
            
    # 내용 입력 (비어있으면 재입력)
    content = ""
    while not content:
        content = input("내용: ").strip()
        if not content:
            print("내용은 비워둘 수 없습니다. 다시 입력해주세요.")
            
    # 카테고리 선택
    print("\n카테고리 선택:")
    for i, cat in enumerate(CATEGORIES, 1):
        print(f"{i}) {cat}")
        
    cat_choice = input("선택 (번호 또는 직접 입력): ").strip()
    
    # 숫자로 선택한 경우
    if cat_choice.isdigit():
        idx = int(cat_choice) - 1
        if 0 <= idx < len(CATEGORIES):
            category = CATEGORIES[idx]
        else:
            category = "기타"
    elif cat_choice in CATEGORIES:
        category = cat_choice
    else:
        category = "기타" # 기본값 또는 직접 입력 처리
        
    # 새로운 프롬프트 딕셔너리 생성 및 추가
    new_item = {
        "title": title,
        "content": content,
        "category": category,
        "favorite": False
    }
    prompts.append(new_item)
    print("\n프롬프트가 추가되었습니다!")

def show_list():
    """전체 프롬프트 목록 출력"""
    print("\n=== 프롬프트 목록 ===")
    if not prompts:
        print("등록된 프롬프트가 없습니다.")
        return
        
    for i, p in enumerate(prompts, 1):
        star = " ⭐" if p["favorite"] else ""
        print(f"{i}. [{p['category']}] {p['title']}{star}")
        
    print(f"\n총 {len(prompts)}개의 프롬프트")

def show_category():
    """카테고리별 프롬프트 조회"""
    print("\n=== 카테고리별 조회 ===")
    for i, cat in enumerate(CATEGORIES, 1):
        print(f"{i}) {cat}")
        
    cat_choice = input("선택: ").strip()
    if cat_choice.isdigit():
        idx = int(cat_choice) - 1
        if 0 <= idx < len(CATEGORIES):
            selected_cat = CATEGORIES[idx]
        else:
            print("잘못된 번호입니다.")
            return
    else:
        print("올바른 번호를 입력해주세요.")
        return
        
    print(f"\n[{selected_cat}] 카테고리 프롬프트:")
    filtered = [p for p in prompts if p["category"] == selected_cat]
    
    if not filtered:
        print("해당 카테고리에 등록된 프롬프트가 없습니다.")
        return
        
    for i, p in enumerate(filtered, 1):
        star = " ⭐" if p["favorite"] else ""
        print(f"{i}. {p['title']}{star}")
    print(f"\n총 {len(filtered)}개의 프롬프트")

def search_prompt():
    """키워드로 제목 또는 내용 검색"""
    print("\n=== 프롬프트 검색 ===")
    keyword = input("검색어: ").strip()
    if not keyword:
        print("검색어를 입력해주세요.")
        return
        
    results = []
    for p in prompts:
        if keyword in p["title"] or keyword in p["content"]:
            results.append(p)
            
    print(f"\n검색 결과:")
    if not results:
        print("검색 결과가 없습니다.")
        return
        
    for i, p in enumerate(results, 1):
        star = " ⭐" if p["favorite"] else ""
        print(f"{i}. [{p['category']}] {p['title']}{star}")
    print(f"\n{len(results)}개의 프롬프트를 찾았습니다.")

def view_detail():
    """프롬프트 상세 보기"""
    print("\n=== 프롬프트 상세 보기 ===")
    if not prompts:
        print("등록된 프롬프트가 없습니다.")
        return
        
    show_list()
    num_str = input("\n번호 입력: ").strip()
    
    if not num_str.isdigit():
        print("숫자로 된 번호를 입력해주세요.")
        return
        
    idx = int(num_str) - 1
    if 0 <= idx < len(prompts):
        p = prompts[idx]
        star = "⭐" if p["favorite"] else "없음"
        print("\n" + "─" * 28)
        print(f"제목: {p['title']}")
        print(f"카테고리: {p['category']}")
        print(f"즐겨찾기: {star}")
        print("─" * 28)
        print(f"내용:\n{p['content']}")
        print("─" * 28)
    else:
        print("존재하지 않는 번호입니다.")

def manage_favorite():
    """즐겨찾기 추가/해제 관리"""
    print("\n=== 즐겨찾기 관리 ===")
    if not prompts:
        print("등록된 프롬프트가 없습니다.")
        return
        
    show_list()
    num_str = input("\n프롬프트 번호 입력: ").strip()
    
    if not num_str.isdigit():
        print("숫자로 된 번호를 입력해주세요.")
        return
        
    idx = int(num_str) - 1
    if 0 <= idx < len(prompts):
        p = prompts[idx]
        p["favorite"] = not p["favorite"] # 토글(True <-> False)
        status = "추가" if p["favorite"] else "해제"
        print(f"'{p['title']}' 프롬프트를 즐겨찾기에 {status}했습니다!")
    else:
        print("존재하지 않는 번호입니다.")

def show_favorites():
    """즐겨찾기 목록 보기"""
    print("\n=== 즐겨찾기 목록 ===")
    favorites = [p for p in prompts if p["favorite"]]
    
    if not favorites:
        print("즐겨찾기에 등록된 프롬프트가 없습니다.")
        return
        
    for i, p in enumerate(favorites, 1):
        print(f"{i}. [{p['category']}] {p['title']} ⭐")
    print(f"\n총 {len(favorites)}개의 즐겨찾기")

def main():
    """프로그램 메인 실행 제어 함수"""
    print("🚀 나만의 프롬프트 관리 프로그램이 실행되었습니다.")
    while True:
        choice = show_menu()
        
        if choice == "1":
            add_prompt()
        elif choice == "2":
            show_list()
        elif choice == "3":
            show_category()
        elif choice == "4":
            search_prompt()
        elif choice == "5":
            view_detail()
        elif choice == "6":
            manage_favorite()
        elif choice == "7":
            show_favorites()
        elif choice == "0":
            print("\n프로그램을 종료합니다. 이용해주셔서 감사합니다! 👋")
            break
        else:
            print("\n[오류] 올바른 메뉴 번호를 입력해주세요. (0~7)")

if __name__ == "__main__":
    main()
2. README.md 작성 가이드
GitHub 저장소 루트 디렉토리에 생성할 README.md 파일의 예시입니다.

Markdown
# 📝 Prompt Manager (나만의 프롬프트 관리 프로그램)

Generative AI 활용 과정에서 파편화되기 쉬운 프롬프트를 체계적으로 기록하고 관리하기 위한 **Python 콘솔 기반 프로그램**입니다.

## 🛠️ 주요 기능
1. **프롬프트 추가**: 제목, 내용, 카테고리를 입력하여 새로운 프롬프트 등록
2. **프롬프트 목록**: 전체 등록된 프롬프트와 카테고리, 즐겨찾기(⭐) 여부 확인
3. **카테고리별 조회**: 텍스트, 이미지, 영상, 페르소나 등 카테고리별 필터링 조회
4. **프롬프트 검색**: 키워드 입력으로 제목 및 내용에 해당하는 프롬프트 검색
5. **상세 보기**: 선택한 프롬프트의 전체 내용 출력
6. **즐겨찾기 관리**: 자주 쓰는 프롬프트 즐겨찾기 지정 및 해제, 목록 모아보기

## 📂 카테고리 안내
* `텍스트 생성`: 블로그, 이메일, 문서 작성 등 텍스트 기반 프롬프트
* `이미지 생성`: 미드저니, DALL-E 등 이미지 썸네일 및 키워드 프롬프트
* `영상 생성`: 영상 스크립트 및 콘셉트 프롬프트
* `페르소나`: 전문가 역할 부여 지시문
* `자동화`: 노코드/워크플로우 연계 프롬프트
* `기타`: 분류되지 않은 프롬프트

## 💻 실행 방법
1. Python 3.10 이상이 설치되어 있는지 확인합니다.
2. 터미널에서 아래 명령어로 프로그램을 실행합니다.
   ```bash
   python prompt_manager.py

---

## 3. Git 커밋 전략 가이드 (최소 10개 커밋 및 브랜치 병합 조건 충족)

미션 요구사항 중 **최소 10개 이상의 의미 있는 커밋**과 **브랜치 생성 및 병합 기록**을 달성하기 위한 작업 순서입니다.

1. **커밋 1**: `git init` 후 기본 파일 구조 생성 (`git commit -m "chore: initialize project repository"`)
2. **커밋 2**: 기본 데이터가 포함된 초기 파이썬 뼈대 작성 (`git commit -m "feat: add initial prompt data and structure"`)
3. **커밋 3**: 메인 메뉴 출력 함수 구현 (`git commit -m "feat: implement main menu interface"`)
4. **커밋 4**: 프롬프트 추가 기능 구현 (`git commit -m "feat: add prompt creation feature"`)
5. **브랜치 생성**: `git checkout -b feature/list`
6. **커밋 5**: 프롬프트 목록 조회 기능 구현 (`git commit -m "feat: implement prompt list view function"`)
7. **커밋 6**: 즐겨찾기 아이콘 표기 기능 추가 (`git commit -m "style: add star icon for favorite prompts in list"`)
8. **브랜치 병합**: `git checkout main` 후 `git merge feature/list` 수행 (`git commit -m "merge: merge feature/list branch into main"`)
9. **커밋 7**: 카테고리별 조회 기능 구현 (`git commit -m "feat: implement category filtering feature"`)
10. **커밋 8**: 프롬프트 검색 기능 구현 (`git commit -m "feat: implement keyword search feature"`)
11. **커밋 9**: 상세 보기 및 즐겨찾기 관리 기능 구현 (`git commit -m "feat: implement detail view and favorite toggle features"`)
12. **커밋 10**: README.md 작성 및 최종 점검 (`git commit -m "docs: write project README and documentation"`)
13. **원격 저장소 연결 및 푸시**: `git remote add origin <URL>`, `git push -u origin main`
